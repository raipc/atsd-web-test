# This workflow will run web tests with Docker

name: ATSD Web Tests in Docker container

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  docker-pull:
    runs-on: ubuntu-latest
    steps: 
      - run: docker://axibase/atsd_package_validation:latest
   
