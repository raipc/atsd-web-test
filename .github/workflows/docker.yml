# This workflow will run web tests with Docker

name: ATSD Web Tests in Docker container

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  docker-pull:
    container: axibase/atsd_package_validation
    runs-on: ubuntu-latest
    steps: 
      - run: echo "Test completed"
   
